package commons;

public enum BrowserList { // define Enum để tránh trường hợp thay đổi dữ liệu - enum là giá trị cố định
	FIREFOX, H_FIREFOX, CHROME, H_CHROME, EDGE, IE, OPERA, COCOC, BRAVE, SAFARI;
}
